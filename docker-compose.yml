version: '3.8'

services:
  mydecks:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/mydecks.db
    restart: unless-stopped
